title: "Design Tools"
description: "High-performance Grasshopper components for industrial design."

# This tells GitHub to pull the community theme directly from its repository
remote_theme: just-the-docs/just-the-docs

# Enable the built-in search engine
search_enabled: true

# Color scheme supports "light" (default) and "dark"
color_scheme: dark
